2. Scatter Plot:
A scatter plot is used to visualize the relationship between two variables.
x = [1, 2, 3, 4, 5];
y = [2, 3, 1, 4, 7];
scatter(x, y);
title('Scatter Plot');
xlabel('X-axis Label');
ylabel('Y-axis Label');